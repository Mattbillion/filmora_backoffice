import { z } from 'zod';

{{#each schemas}}
export const {{this.schemaName}} = {{{this.schemaString}}};

export type {{this.schemaTypeName}} = z.infer<typeof {{this.schemaName}}>;

{{/each}}

export const RVK_{{rvkConst}} = '{{kebabCase service}}';
